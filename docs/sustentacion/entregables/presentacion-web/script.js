const all=[...document.querySelectorAll('.slide')];let showBackup=false;let slides=[];let i=0;const c=document.getElementById('counter');function visible(){slides=all.filter(s=>showBackup||!s.classList.contains('backup'));if(i>=slides.length)i=slides.length-1}function show(n){visible();slides.forEach(s=>s.classList.remove('active'));i=Math.max(0,Math.min(n,slides.length-1));slides[i].classList.add('active');c.textContent=(i+1)+' / '+slides.length;location.hash=slides[i].dataset.no}function next(){show(i+1)}function prev(){show(i-1)}document.getElementById('next').onclick=next;document.getElementById('prev').onclick=prev;document.getElementById('toggleBackup').onclick=()=>{showBackup=!showBackup;document.body.classList.toggle('showBackup',showBackup);show(i)};document.getElementById('fullscreen').onclick=()=>document.fullscreenElement?document.exitFullscreen():document.documentElement.requestFullscreen();addEventListener('keydown',e=>{if(['ArrowRight','PageDown',' '].includes(e.key)){e.preventDefault();next()}if(['ArrowLeft','PageUp','Backspace'].includes(e.key)){e.preventDefault();prev()}if(e.key.toLowerCase()==='b'){showBackup=!showBackup;show(i)}if(e.key.toLowerCase()==='f'){document.getElementById('fullscreen').click()}});const h=Number(location.hash.replace('#',''));if(h){showBackup=h>18;visible();const idx=slides.findIndex(s=>Number(s.dataset.no)===h);show(idx>=0?idx:0)}else show(0);